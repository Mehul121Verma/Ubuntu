autoinstall:
    version: 1
    identity:
        realname: "Toast User"
        username: toastuser
        password: "$1$uQvy6o6V$K2F2D91R3SXMbV0CWQ5H30" # Password hash
        hostname: computer-name
    storage:
        layout:
            name: lvm
            sizing-policy: all
            password: "toastuser" # Full-disk encryption passphrase
    fallback: abort
    geoip: true
    codecs:
        install: true
    drivers:
        install: true
    shutdown: reboot
